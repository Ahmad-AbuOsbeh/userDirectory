class Content {
	constructor() {
		this.sortableListUI = new sortableListUI('badgeList');
	}

	init() {
		// this.badges.get((error, badges) => {
		// 	if (error) return console.error(error);

		// 	// this.badgeList
		// });
	}
}